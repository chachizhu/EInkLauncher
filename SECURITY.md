# 安全政策

## 受支持版本

E-Ink Launcher 通过 GitHub Releases 直接分发 APK，不通过 Google Play 分发。安全修复只面向本仓库发布的最新稳定版；报告问题前请先确认已安装最新版本，并核对 APK 的签名证书与 [README](README.md) 中公布的 SHA-256 指纹一致。

以下版本不在安全支持范围内：

- 第三方重新签名、修改或重新打包的 APK；
- 从非本仓库 GitHub Releases 渠道取得的 APK；
- 已被更新版本取代的旧版本；
- root、系统框架修改或厂商安全补丁缺失导致的 Android 平台问题。

Android 5.0（API 21）是应用的最低兼容版本，不代表旧设备仍能获得厂商或 Android 平台安全更新。在不再接收系统安全补丁的设备上，应把 E-Ink Launcher 视为本地桌面工具，不应据此存放或处理额外敏感数据。

## 安全边界

- 应用只在用户主动操作后检查、下载或安装更新，不执行静默安装，不运行后台更新服务。
- 更新下载仅接受项目允许的 GitHub Release 地址和 GitHub 发布文件 CDN 重定向，并限制文件大小。
- 下载后会校验 SHA-256、包名、版本号和 APK 签名；只有比当前安装版本更新、且签名证书一致的 APK 才会交给 Android 系统安装器。
- 更新文件只通过非导出的 `FileProvider` 从应用私有缓存授予系统安装器临时读取权限。
- 最终的未知来源授权和安装确认由 Android 系统完成。用户可以不授予此权限，改为在浏览器中手动下载。

APK 平台签名是直装发布的身份根。Release 中的 `.sha256` 文件用于发现下载损坏，但不能独立证明发布者身份。发布工作流会将 APK 实际签名指纹与仓库内固定信任值及 README 公布值同时比较，并拒绝覆盖已经存在的 Release；仓库还必须启用 GitHub 的 **Immutable releases**，为启用后新发布 Release 的 tag 和资产提供服务端不可变保护。

## 隐私边界

应用不包含账号、广告、分析、崩溃上报或云同步。可启动应用列表和桌面选择只在设备本地处理，不会由应用上传。用户主动检查或下载更新时，GitHub 及其 CDN 会收到提供 HTTPS 服务所需的常规网络元数据。完整说明见 [README 的隐私章节](README.md#隐私)。

普通 Issue 中请只提供定位问题所必需的信息。提交日志前应删除账号、设备标识符、文件路径、已安装应用清单及其他个人数据。

## 报告安全问题

请优先通过仓库 Security 页面中的 **Report a vulnerability** 使用 GitHub Private Vulnerability Reporting 私下报告。若该入口不可用，请只创建一个不含漏洞细节的公开 Issue，请求维护者建立私密联系渠道。

报告中建议包含：

- 受影响版本、Android 版本和设备型号；
- 可复现步骤、预期结果与实际结果；
- 影响范围和必要的最小化证明；
- 已知缓解措施；
- 是否存在公开利用或计划披露日期。

不要提交真实私钥、keystore、密码、访问令牌、个人数据或不必要的完整设备日志。维护者会先确认影响与复现条件，再协调修复版本和披露时间。修复将以更高版本号的新 Release 发布；既有 APK 和 Release 资产不会被替换。
